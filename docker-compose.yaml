services:
  ai-model:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: eco_wise_model_api
    ports:
      - '8000:8000'
    env_file:
      - .env
    restart: unless-stopped
